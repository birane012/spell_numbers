/*
import 'package:flutter_test/flutter_test.dart';

import 'package:spell_numbers/spell_numbers.dart';

void main() {
  test('adds one to input values', () {
    final calculator = SpellNumber();
    expect(calculator.spellNumber(2), 3);
    expect(calculator.spellNumber(-7), -6);
    expect(calculator.spellNumber(0), 1);
  });
}
*/
